{
  "name": "fashionflat-ai",
  "version": "1.0.0",
  "description": "AI-Powered Flat Sketch Generator for Fashion Designers",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "test": "cd frontend && npm test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "@huggingface/inference": "^2.8.1",
    "@vercel/node": "^3.0.12"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
